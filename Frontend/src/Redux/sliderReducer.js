// import { FETCH_SLIDER_PRODUCTS } from '../Redux/sliderActions';

// const initialState = {
//   products: [],
// };

// const sliderReducer = (state = initialState, action) => {
//   switch (action.type) {
//     case FETCH_SLIDER_PRODUCTS:
//       return { ...state, products: action.payload };
//     default:
//       return state;
//   }
// };

// export default sliderReducer;
